{
  "name": "pizza-store",
  "private": true,
  "scripts": {
    "build": "cd shared && npm ci && npm run test:run && cd client && npm ci && npm run build"
  },
  "dependencies": {
    "next": "15.3.4"
  }
}
